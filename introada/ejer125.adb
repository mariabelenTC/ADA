A:=new Frecuency_Array('a'..'g');
for i in b.all'range oop
b.all(i):=0;
end loop;

--RODAJAS
c.all('a' .. 'g'):=a.all; --mete los valores del array A en la franja de C

procedure Free is new Ada.Uncheked_Deal..
()